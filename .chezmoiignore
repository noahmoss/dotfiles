# Never apply these files
.DS_Store
*.swp
setup.sh
README.md

# Machine-specific, never managed by chezmoi
.bashrc.local

# Caches and transient state
**/.cache
**/.cpcache
**/undodir

# Vim plugins — installed via :PlugInstall, not tracked
dot_vim/plugged

# Lazy.nvim writes to this on every plugin update; let each machine
# manage its own. Re-add manually if you want to pin versions.
dot_config/nvim/lazy-lock.json

# Node modules — installed via npm/package.json, not tracked
**/node_modules
